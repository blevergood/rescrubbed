# Generated by the Niobium FHE Application Design AI Assistant (FHEanna).
# Build tree
/build/
# Per-run homes provisioned by run_test.sh and the two-process demo (keys,
# ciphertexts). Directory-only glob: covers every run mode, and cannot match
# run_test.sh.
/run_*/
client_home/
server_home/
# Local tooling that must never be committed
.claude/
.agents/
.venv/
__pycache__/
*.pyc
# Toolchain replay artifacts
/nbcc_fhetch_replay_source_*/
/fhetch_driver_source_*/
# This application's trace directories
/readmit_server_workload_*/
/readmit_profile_*/
# Derived from the public dataset and regenerated by `make data`; not sources.
# data/test_records.csv IS tracked: it is the test set the published results are
# quoted against, and it is small.
/data/raw_encounters.csv
/data/dataset_metadata.txt
/data/test_features.npy
/data/test_labels.npy
